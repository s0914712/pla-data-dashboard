name: Weather Report

on:
  # 定時執行 - 每天 UTC 00:00 和 12:00（台灣時間 08:00 / 20:00）
  schedule:
    - cron: '0 0,12 * * *'

  # 允許手動觸發
  workflow_dispatch:

  # Push 到 main 時執行（scraper 或 workflow 變更時）
  push:
    branches:
      - main
    paths:
      - 'scripts/scrapers/weather_scraper.py'
      - '.github/workflows/weather.yml'

jobs:
  fetch-weather:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pandas

      - name: Run weather scraper
        env:
          WINDY_API_KEY: ${{ secrets.WINDY_API_KEY }}
        run: python scripts/scrapers/weather_scraper.py

      # （只在你要 debug 時打開）
      # - name: Debug output files
      #   run: ls -R

      - name: Commit and push CSV results
        run: |
          set -e

          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          # 若 data 資料夾不存在或沒有 CSV，則跳過
          if [ -d data ] && ls data/*.csv >/dev/null 2>&1; then
            git add data/*.csv
            git diff --staged --quiet || git commit -m "📊 Update airport weather data - $(date +'%Y-%m-%d %H:%M') UTC"
            git push
          else
            echo "⚠️ No CSV files found in data/, skipping commit"
          fi
